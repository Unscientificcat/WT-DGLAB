# 开发执行步骤

## 总体阶段

| 阶段 | 内容 | 状态 |
|------|------|------|
| 1 | 基础框架 — 主窗口、主题、配置管理 | ✅ 完成 |
| 2 | 游戏数据模块 — HTTP 轮询、数据解析 | ✅ 完成 |
| 3 | 郊狼控制模块 — WebSocket、QR码、心跳 | ✅ 完成 |
| 4 | 映射引擎 + 设置面板联调 | ✅ 完成 |
| 5 | 整合测试 + 目录化打包与 ZIP 发布 | ✅ 完成 |
| 6 | `.pulse` 自定义波形与随机策略重构 | 🚧 进行中 |
| 7 | 浅色磨砂玻璃 UI 重构 | ✅ 完成 |
| 8 | 实时输出波形显示 + 悬浮窗内容可调 | ✅ 完成 |

### 阶段6：波形重构 🚧
- [x] 建立 `waveforms` 目录与官方明文 `.pulse` 解析器
- [x] 统一 V3/V4 8 字节帧及自定义波形播放器
- [x] 配置字段迁移与独立波形设置窗口
- [x] 常规时间范围随机、事件触发随机和 CAS 配置接入
- [ ] 实际设备回归与 PyInstaller 目录包验证

### 阶段7：玻璃主题 UI 重构 ✅
- [x] 背景目录、默认壁纸、模糊缓存和异常回退
- [x] 主窗口双层玻璃面板与外观设置页
- [x] 强度平滑动画、事件呼吸边框和透明滚动区域
- [x] 配置持久化、打包资源和玻璃主题离屏测试
- [x] 全量测试与 PyInstaller 目录包验证

### 阶段8：实时输出波形显示 + 悬浮窗内容可调 ✅
- [x] `OutputTelemetry` 线程安全输出遥测：V3/V4 控制器记录实际下发波形批次与当前波形名
- [x] 仪表盘 A/B 通道卡与悬浮窗实时显示当前输出波形名（随机/事件/CAS 换波跟随）
- [x] 点击通道卡打开单通道输出电压曲线窗口（滚动 6 秒、0-200 电压刻度、100ms 刷新）
- [x] 悬浮窗 8 项显示内容开关（模式/过载/速度/事件/A强度/A波形/B强度/B波形，默认全开）
- [x] 设置入口：主窗口悬浮窗卡片按钮 + 悬浮窗右键菜单"显示内容…"，即时生效并持久化
- [x] 新增回归测试 33 项，全量 `155 passed`

## 阶段详细

### 阶段1：基础框架 ✅
- [x] 创建项目目录结构
- [x] 实现 ConfigManager（JSON 配置读写）
- [x] 定义浅色 PySide6 / QSS 主题
- [x] 实现 MainWindow 布局（状态栏+面板+QR区）
- [x] 实现 SettingsPanel（所有参数设置控件）
- [x] 实现 Dashboard（实时数据展示面板）
- [x] 实现 StatusBar（连接状态指示）
- [x] 实现 QRWidget（QR 码区域）
- [x] 创建 main.py 入口（App 类集成各模块）
- [x] 验证 GUI 正常启动显示

### 阶段2：游戏数据模块 ✅
- [x] 实现 GameReader HTTP 轮询
- [x] 解析飞机过载数据（ny 字段）
- [x] 解析坦克损伤数据（indicators + state 双重提取）
- [x] 连接 Dashboard 实时显示
- [x] auto-detect 载具类型
- [x] 异常处理（WT未运行、超时、JSON解析失败）
- [x] 独立 EventDetector（跨局 HUD 游标、击杀/死亡、维修边沿）

### 阶段3：郊狼控制模块 ✅
- [x] 研究 DG-LAB Socket 协议（基于 PyDGLab-WS 库）
- [x] 实现 WebSocket 服务端（asyncio 后台线程）
- [x] 实现绑定/心跳/强度消息（由 PyDGLab-WS 封装）
- [x] 实现 QR 码生成（qrcode + PIL → Qt QPixmap）
- [x] 线程安全通信（queue.Queue + asyncio.run_coroutine_threadsafe）
- [x] V3/V4 App 用户选择、配置迁移和连接重建
- [x] V4 Relay、官方二维码、设备快照、强度与波形控制
- [x] V3 本机服务和 V4 官方 Relay 握手验证
- [ ] V4 实际设备测试（需要 4.x App + 郊狼设备）

### 阶段4：映射引擎 ✅
- [x] 实现 MappingEngine 线性映射
- [x] 解耦飞机映射和坦克映射为独立方法
- [x] 连接 GameReader → MappingEngine → CoyoteController
- [x] 设置面板参数在轮询中实时生效
- [x] 异常处理（边界值、除零保护）

### 阶段5：整合与打包 🚧
- [x] 端到端模块集成（main.py App 类）
- [x] 异常状态处理（WT未运行 → 零强度、设备断连 → 忽略指令）
- [x] PyInstaller 目录化打包脚本（build.py）
- [x] 生成安全默认配置模板和外置 resources 目录
- [x] 生成并校验目录版 EXE 与 ZIP 发布包
- [x] 自动回归测试（基础 37 项；托盘更新后 41 项；单实例更新后 44 项）
- [x] 实际 PyInstaller 打包（当前产物 `WT-DGLAB v1 beta_4.exe`）
- [x] 重写 GitHub README，并补充 `v1 beta_1` 相对 `v1 beta` 的更新日志
- [ ] 游戏内实测（需要战争雷霆 + 郊狼设备）

### 2026-08-15：v1 beta_1 发布整理 ✅
- [x] 统一程序窗口标题、Windows 应用标识和发布版本号为 `郊狼雷霆 v1 beta_1`
- [x] 将 PyInstaller 产物命名为 `WT-DGLAB v1 beta_1.exe`
- [x] README 和开发日志补充今晚的托盘、持久化、单实例、维修恢复及 CAS 更新
- [x] 整理源码、文档、测试、图标和新版 EXE 到 `release`
- [x] 根目录与 `release` 自动化测试均通过 `51 passed`

### 2026-08-15：托盘、配置持久化与图标更新 🚧
- [x] 确认交互和配置存储方案
- [x] 建立专项开发文档
- [x] 点击关闭按钮后隐藏到系统托盘
- [x] 托盘恢复窗口和真正退出程序
- [x] 配置保存到 EXE 同目录且缺失时自动生成
- [x] 关闭到托盘和托盘退出时自动保存当前设置
- [x] 使用 `tubiao1.jpg` 替换全部程序图标
- [x] 自动测试与 PyInstaller 打包（托盘阶段根目录与 `release` 均为 41 项通过）

### 2026-08-15：单实例与重复启动 🚧
- [x] Windows 命名互斥锁阻止第二业务实例
- [x] Qt 本地 IPC 通知已有实例显示主窗口
- [x] 隐藏到托盘后重复启动可以触发恢复
- [x] 激活请求应答和启动竞态重试
- [x] 新增单实例回归测试（完整测试 44 项通过）
- [x] PyInstaller 重新打包与双进程实机验证

### 2026-08-15：维修事件覆盖恢复 🚧
- [x] 定位维修边沿被击杀优先级消费的根因
- [x] 击杀/死亡结束后重新检查当前维修状态
- [x] 持续维修时立即恢复维修强度和波形
- [x] 新增维修覆盖场景回归测试（完整测试 47 项通过）
- [x] 同步 `release` 与 PyInstaller 重新打包

### 2026-08-15：陆战 CAS 触发开关与事件修复 🚧
- [x] 增加可持久化的“启用 CAS 触发”开关
- [x] 关闭 CAS 时同时禁止过载和击杀/坠毁事件输出
- [x] 陆战 CAS 复用陆战事件配置并保留飞机事件显示语义
- [x] 增加 CAS 配置、事件选择和输出门控测试（完整测试 51 项通过）
- [x] 同步 `release` 与 PyInstaller 重新打包

### 2026-08-17：HUD 事件多语言适配 ✅
- [x] 根据战争雷霆官方本地化词条整理七种目标语言的击落、击毁和坠毁格式
- [x] 自动识别英语、俄语、法语、德语、简体中文、繁体中文和日文
- [x] 区分主动与被动叙述，保留击杀方和被击杀方判定
- [x] 统一 Unicode、零宽字符、空白和拉丁文字大小写
- [x] 新增多语言、真实 8111 格式和误触发回归测试，根目录完整测试 `98 passed`
- [x] 用户游戏内验证通过
- [x] 同步 `release` 并重新打包 `WT-DGLAB v1 beta_1.exe`

### 2026-08-31：稳定性与安全退出加固 ✅
- [x] 对配置文件增加类型、范围和有限数值校验，损坏配置自动回退默认值
- [x] 对坦克遥测异常数值做字段级安全回退
- [x] V3/V4 停止流程先执行通道归零，再等待后台清理
- [x] 清理 V3/V4 启动结果队列，避免超时重试消费旧结果
- [x] 新增异常配置与异常遥测回归测试，完整测试 `101 passed`
- [ ] V3/V4 实际手机与郊狼设备断开、退出归零联调
- [x] 新 EXE 离屏启动检查通过，PyInstaller 父/子进程均正常响应

### 2026-08-17：v1 beta_2 发布整理 ✅
- [x] 将窗口标题、Windows 应用标识和发布版本统一更新为 `WT-DGLAB v1 beta_2`
- [x] README 新增 `v1 beta_2` 多语言适配更新日志
- [x] 根目录和 `release` 完整自动测试均为 `98 passed`
- [x] 生成、同步并启动检查 `WT-DGLAB v1 beta_2.exe`
- [x] 保留 beta_1 旧产物，根目录与 `release` 的 beta_2 EXE SHA-256 一致

### 2026-09-02：v1 beta_3 发布整理 ✅
- [x] 统一窗口标题、应用标识和构建版本为 `郊狼雷霆 v1 beta_3`
- [x] 生成并验证 `dist/WT-DGLAB v1 beta_3/` 目录包及 ZIP

### 2026-09-05：v1 beta_4 发布整理 ✅
- [x] 增加卡片不透明度滑条（20%–90%，默认 72%）并即时保存
- [x] 统一窗口标题、应用标识和构建版本为 `郊狼雷霆 v1 beta_4`
- [x] 完成玻璃主题配置、回归测试和发布包生成

### 2026-09-06：悬浮窗大小滑块线性调节（v1 beta_5）✅
- [x] 悬浮窗"大/中/小"三档改为 13–52px 滑块无级调节（主字号，通道/小字按 0.5/0.375 比例跟随）
- [x] 主窗口设置卡片与悬浮窗右键面板均改为滑块，拖动实时生效、两处同步
- [x] 配置迁移：`overlay_size`（"小/中/大"）→ `overlay_size_px`（19/26/40），保存后写新键
- [x] 修复悬浮窗字号切换后 `adjustSize` 取到旧布局尺寸的滞后问题（刷新布局事件后再取尺寸）
- [x] 修复 `tests/test_overlay_size.py` 单独运行缺 QApplication 崩溃的隐藏缺陷
- [x] 全量回归 122 passed；统一版本为 `郊狼雷霆 v1 beta_5` 并生成发布包

### 2026-09-06：实时输出波形显示 + 悬浮窗内容可调（v1 beta_6）✅
- [x] 新增 `src/output_telemetry.py` 线程安全输出遥测记录器，V3/V4 控制器记录实际下发批次与当前波形名
- [x] 仪表盘 A/B 通道卡与悬浮窗实时显示当前输出波形名
- [x] 点击通道卡打开单通道输出电压曲线窗口（最近 6 秒滚动、0-200 电压刻度与强度条一致、100ms 刷新）
- [x] 悬浮窗 8 项显示内容开关 + 设置对话框（主窗口按钮与右键菜单两处入口，即时生效并保存）
- [x] 新增测试 33 项，全量回归 `155 passed`
- [x] 统一版本为 `郊狼雷霆 v1 beta_6`（build.py、main.py、窗口标题、AppUserModelID、README）
- [ ] 实机波形验证（切换波形/随机换波时曲线窗口与名称跟随，需要实际郊狼设备）

### 2026-09-06：关于对话框（beta_6）✅
- [x] 新增 `src/version.py`：`APP_NAME` / `APP_VERSION` 版本单一来源，窗口标题与 `build.py` 统一引用
- [x] 新增 `src/gui/about_dialog.py`：模态"关于"对话框（图标、应用名与版本、作者"某不科学的猫酱"、B 站与 GitHub 可点击链接、MIT 许可与非官方声明）
- [x] 顶部状态栏"注意事项"旁新增"关于"文字按钮入口
- [x] 新增 `tests/test_about_dialog.py` 6 项，全量回归 `164 passed`
- [x] 文档同步：design-spec 新增"关于对话框"一节，README 通用能力补说明

## 开发原则
- 每个阶段完成后验证再进入下一阶段
- 每个模块保持独立，通过明确接口通信
- 所有用户可见字符串使用中文
- 代码注释使用中文

## 技术方案（最终采用）
- GUI: PySide6（Qt 桌面界面，详见 `docs/ui-redesign.md`）
- 郊狼 V3: pydglab-ws 本机服务端
- 郊狼 V4: 按官方 V4 RPC 帧实现的 Relay 客户端
- 游戏数据: requests 轮询 localhost:8111
- 打包: PyInstaller `--onedir` → 版本目录 + ZIP

### 2026-09-07：仪表盘模式切换迁移 ✅
- [x] 仪表盘实时状态卡增加空战/陆战上下模式按钮，当前模式高亮
- [x] 业务模式状态迁移至 Dashboard，重复点击去重，切换后立即持久化
- [x] 触发设置中的空战/陆战按钮改为“空战设置”“陆战设置”，仅切换查看页
- [x] 保持业务模式与设置查看页独立，波形/背景页往返不重置状态
- [x] 新增 8 项模式切换回归测试
- [x] 全量回归 `175 passed`
