# 郊狼雷霆 v1.0 更新公告

> 将《战争雷霆》实时遥测与 DG-LAB 郊狼 3.0 联动的 Windows 桌面程序。
>
> 项目主页：https://github.com/Unscientificcat/WT-DGLAB

**郊狼雷霆 v1.0 正式版来了！**

从第一行代码到今天，郊狼雷霆经历了 14 篇开发日志、8 个开发阶段、175 项自动化测试，终于摘掉了 beta 帽子。感谢每一位在测试期提出反馈的玩家，这版是给你们攒出来的。

---

## ✨ 这版最大的亮点

**全新的浅色磨砂玻璃界面**
界面整体重做为玻璃拟态风格：默认壁纸打底，半透明玻璃卡片浮在上面，卡片不透明度可以自由调节，也支持换成自己喜欢的背景图。空战和陆战的模式切换也挪到了仪表盘上，点一下就切，立即生效。

**实时输出波形显示**
现在你能在 A/B 通道卡片和悬浮窗上直接看到当前正在输出的波形名字，随机换波、事件换波、空战切换都会同步刷新。点击通道卡片还能打开一条滚动约 6 秒的实时电压曲线，实际下发了什么一目了然。

**悬浮窗完全由你定义**
悬浮窗上的每一块内容——模式、实时过载、实时速度、事件提示、A/B 强度、A/B 波形名，共 8 项都可以单独开关；大小也从三档固定改为无级滑块，想多大调多大。

**击杀事件 7 国语言自动识别**
无论《战争雷霆》客户端是英语、俄语、法语、德语、简中、繁中还是日语，击杀、被击落、坠毁事件都能自动识别，不用手动设置语言。

**DG-LAB 3.x / 4.x App 双支持**
老版 3.x App 走本机 WebSocket 直连，新版 4.x App 走官方 Socket Relay 中继，界面里选择即可，保存后自动重连并刷新二维码。

**自定义波形**
支持 DG-LAB 官方明文 `.pulse` 波形文件，放进 waveforms 目录即可在界面中选用，A/B 通道可各自随机切换，事件期间自动覆盖常规波形。

**体贴的细节**
系统托盘常驻、重复启动自动唤回已有窗口、配置自动保存与损坏自愈、断线安全归零——这些 beta 期间打磨的稳定性改进全部保留。

---

## 📋 完整更新列表（自 v1 beta 以来）

### v1.0（本次）

- 界面切换至浅色磨砂玻璃主题：默认壁纸、玻璃卡片、亮度滤罩，卡片不透明度 20%–90% 可调，支持自定义背景图并即时持久化。
- 空战/陆战业务模式切换移至仪表盘实时状态卡，当前模式高亮，切换立即写入配置。
- 新增实时输出遥测：通道卡与悬浮窗显示当前输出波形名，随机换波、事件换波、CAS 切换同步跟随。
- 点击通道卡打开单通道输出电压曲线窗口（滚动约 6 秒，0-200 电压刻度，A 蓝/B 青）。
- 悬浮窗显示内容 8 项开关（模式/实时过载/实时速度/事件提示/A 强度/A 波形名/B 强度/B 波形名），主窗口与右键菜单两处入口。
- 悬浮窗大小改为 13–65px 无级滑块，旧配置自动迁移。
- 新增"关于"对话框：版本号、作者、B 站与 GitHub 链接、开源许可。
- 版本号统一为 `v1.0`，由 `src/version.py` 单一来源管理。

### v1 beta_6

- 仪表盘 A/B 通道卡与悬浮窗实时显示当前输出波形名。
- 点击通道卡打开单通道输出电压曲线窗口。
- 新增悬浮窗显示内容设置（8 项开关）。
- 修复空战模式误显示速度、未进战斗时显示占位符、主菜单状态灯误报等问题。

### v1 beta_5

- 悬浮窗大小"大/中/小"三档改为无级滑块调节。
- 修复字号切换后窗口尺寸滞后、波形设置按钮未汉化等界面问题。

### v1 beta_4

- 全新浅色磨砂玻璃 UI 主题（glassmorphism），运行时开销近零。
- 新增外观设置页：背景开关、自定义背景图。
- 通道强度平滑动画、事件呼吸边框。
- 修复发布包 QtWidgets DLL 加载失败问题（PyInstaller 误收遮蔽系统 ICU 的 DLL，打包脚本已自动清理并加回归检查）。

### v1 beta_3

- 波形系统重构：仅保留内置"恒定"波形，支持 DG-LAB 官方明文 `.pulse` 文件解析、目录扫描与错误汇总。
- V3/V4 共用波形目录，A/B 通道独立随机换波任务，事件期间覆盖常规波形。
- 新增独立"波形设置"窗口。

### v1 beta_2

- HUD 事件新增英语、俄语、法语、德语、简体中文、繁体中文、日文共 7 种语言自动识别。
- 区分主动/被动事件格式，Unicode 标准化清理，普通损伤与点燃不误触发。

### v1 beta_1

- 新增系统托盘：关闭隐藏到托盘，双击恢复，托盘退出。
- 配置持久化加固：保存到 EXE 同目录，缺失自动生成，原子写入，异常回退默认值。
- Windows 单实例：重复启动自动恢复并激活已有窗口。
- 修复维修事件被击杀覆盖后不能恢复的问题。
- 新增陆战 CAS 触发开关，可独立控制上飞机后的全部输出。

### 稳定性（beta 期间累计）

- 配置加载类型/范围校验，异常配置自动回退默认值。
- V3/V4 停止前 A/B 通道安全归零，断线、退出、无效对局均强制归零。
- 修复跨对局事件串线、训练场残留速度导致持续输出、事件时序错误等多个问题。
- HTTP 轮询移出 GUI 线程，`127.0.0.1` 替代 `localhost` 消除 DNS 延迟。

---

## ⬇️ 下载与使用

1. 从 [GitHub Releases](https://github.com/Unscientificcat/WT-DGLAB/releases) 下载 `WT-DGLAB v1.0.zip` 并完整解压。
2. 运行目录中的 `WT-DGLAB.exe`，阅读并确认注意事项。
3. 选择 V3 或 V4 App，用手机扫码连接。
4. 所有强度从 `0` 开始，确认一切正常后再逐步增加。

> [!WARNING]
> 本软件控制电刺激设备，使用不当可能造成人身伤害。未成年人、孕妇、心脏疾病患者及植入医疗设备者严禁使用。首次使用前务必完整阅读《注意事项》。

DG-LAB 4.x App 与实际郊狼设备的完整实机联调仍在进行中，遇到问题欢迎到 GitHub 提 Issue。

---

*郊狼雷霆基于 MIT 协议开源，作者：某不科学的猫酱。*
