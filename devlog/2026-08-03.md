# 开发日志 — 2026-08-03

## 任务

将现有 tkinter UI 重设计并迁移至 PySide6，保持现有业务功能和配置兼容。

## 已完成

- [x] 完成现有 UI、配置和主控制器接口盘点。
- [x] 确认视觉方向：浅色、轻二次元、默认适配 1080p。
- [x] 确认范围：主窗口、悬浮窗、注意事项对话框与二维码流程均重做；不新增业务功能。
- [x] 创建 `docs/ui-redesign.md`，定义迁移边界、布局、接口和验证标准。
- [x] 审查用户 Python 环境：Python 3.14.3 与本项目所需依赖均已安装，PySide6 无界面初始化成功。
- [x] 将 `styles.py`、`main_window.py`、`overlay.py`、`disclaimer_dialog.py` 迁移至 PySide6。
- [x] 重做主窗口为浅色三栏布局：实时状态、模式参数和二维码连接区。
- [x] 重做悬浮窗和注意事项对话框，保留拖动、尺寸选择、关键字高亮与模态确认。
- [x] 调整 `main.py` 的 Qt 生命周期与二维码显示，保持业务模块不变。
- [x] 修复空战波形设置未从 JSON 重载的问题。
- [x] 更新 `requirements.txt` 和 `build.py` 的 PySide6 依赖与收集规则。
- [x] 完成语法编译、Qt 冒烟、完整应用离屏启动、配置往返和原生 1280 x 780 布局验证。
- [x] 禁用数值框和波形下拉框的滚轮修改，避免滚动设置页时误改参数；回归测试通过。
- [x] 修复悬浮窗开关与大小选择未即时生效的问题，并改为透明无背景显示；离屏开关、尺寸和 Alpha 测试通过。
- [x] 修复注意事项对话框关闭时的递归拒绝信号，连续打开/关闭 8 次测试通过。
- [x] 将 WebSocket 端口和刷新间隔移至右侧连接郊狼面板，连接参数保存和 1280 x 780 布局验证通过。
- [x] 修复 HUD 历史击杀在启用提醒后重复触发的问题：初始化游标并跳过旧 ID，模拟回归测试通过；已停止问题运行实例 PID 10180。
- [x] 修复游戏数据断开时 HUD 历史记录仍反复触发的问题：仅在连接和游标均就绪后检测事件；完整回归通过，已停止问题实例 PID 13496。
- [x] 修复 Qt 迁移残留导致的事件 UI 刷新异常：击杀倒计时可从 5.0s 正常递减并结束；完成 WT 8111 与 DG-LAB V3/V4 官方文档复核。

## 进行中

- 无。

## 已完成（补充：跨对局诊断）

- [x] 临时增加 `wt-dglab-trace.log`，记录遥测切换、HUD 基线、事件队列和 UI 消费时序；用户确认跨对局流程恢复正常后，移除文件日志和额外诊断输出。

## 已完成（补充：新对局首帧事件）

- [x] 修复重进对局时通过全量 HUD 重建游标而跳过首批新事件的问题：持续消费跨局 `lastDmg` ID，菜单不触发事件，对局内直接读取增量事件；新局首轮有效遥测即可检查事件。9 项回归测试通过。

## 已完成（补充：跨对局事件）

- [x] 修复退出后重新进入对局时，跨局 HUD 历史和待处理事件可能被新对局消费，进而触发“已离开有效对局”取消日志的问题：离开时清空事件队列并作废 HUD 基线，重进时从当前最高 ID 重建基线，事件检测直接使用本轮状态。8 项回归测试通过。

## 已完成（补充：事件时序）

- [x] 修复事件刚入队时 UI 以空 `GameState()` 覆盖最后有效遥测，导致安全归零分支立即取消击杀、被击落和维修事件的问题：无新遥测时复用最后已验证状态，实际收到无效遥测才会归零。新增时序回归后共 5 项测试通过。

## 已完成（补充）

- [x] 修复退出陆战训练场后 8111 `/indicators` 残留速度导致的持续输出：菜单实测 `/state.valid=false`、`/map_info.json.valid=false`，而指标接口残留 `speed=68`。现以 `map_info.json` 的对局有效标记为安全门槛；无效时停止读取指标、取消事件、清空仪表和悬浮窗，并立即发送 A/B `0`。
- [x] 新增 `tests/test_telemetry_safety.py`：菜单残留拦截、完整地图元数据兼容、事件取消归零与悬浮窗不复用旧值共 4 项回归测试通过；Python 语法检查和离屏应用启动/关闭检查通过。

## 待办

- [ ] 经确认后执行 PyInstaller 重打包（会覆盖 `WT-DGLAB.exe`）。
- [ ] 使用战争雷霆、手机 App 和郊狼 3.0 完成实机联调。
