# 开发日志 - 2026-08-17

## HUD 事件多语言适配

- [x] 确认 `/hudmsg` 的 `damage` 记录只提供本地化 `msg`，没有结构化击杀方与受害方字段。
- [x] 从 War Thunder Datamine 的官方本地化表整理英语、俄语、法语、德语、简体中文、繁体中文和日文词条。
- [x] 将事件解析划分为主动击落/击毁、被动击落/击毁和自行坠毁三类规则。
- [x] 被动短语优先匹配，避免 `shot down by` 被 `shot down` 误判。
- [x] HUD 文本增加 Unicode NFKC、零宽字符清理、空白折叠和大小写归一。
- [x] 补充七种语言的击杀、死亡、坠毁、被动格式及普通损伤/点燃误触发测试。
- [x] 使用已捕获的简中、日文和英文 8111 格式补充军团标签、特殊符号和隐藏字符回归。
- [x] 事件检测测试 `60 passed`，根目录完整测试 `98 passed`，静态编译通过。

## 待办

- [x] 用户确认游戏内验证通过。
- [x] 同步本次源码、测试与文档到 `release`，发布目录完整测试 `98 passed`。
- [x] 使用 Python 3.14.3 / PyInstaller 6.20.0 重新打包 `WT-DGLAB v1 beta_1.exe`。
- [x] 完成新 EXE 离屏启动检查，PyInstaller 父/子进程均正常响应且无残留进程。
- [x] 根目录和 `release` 的 EXE SHA-256 一致：`0258D08BDC6932732AA2E01D0D629A9D65FF756ACE3F3B2412D1AE2C15026BDA`。

## v1 beta_2 发布整理

- [x] 将主窗口标题与 Windows AppUserModelID 升级为 `v1 beta_2`。
- [x] 将打包脚本产物名更新为 `WT-DGLAB v1 beta_2.exe`。
- [x] README 徽章、安装指引、防火墙说明、构建说明和发布建议统一更新为 `v1 beta_2`。
- [x] README 新增 `v1 beta_2` 更新日志，归档七种游戏语言的 HUD 事件适配。
- [x] 根目录与 `release` 完整测试均为 `98 passed`，静态编译通过。
- [x] 生成并同步 `WT-DGLAB v1 beta_2.exe`，保留 beta_1 旧产物。
- [x] beta_2 EXE 离屏启动检查通过，PyInstaller 父/子进程均正常响应且无残留进程。
- [x] beta_2 EXE 大小为 `280149329` 字节，SHA-256 为 `E053C9ED4828161FEF9BE8D742EEB9021371D64B2E88533E841E4930CAA24ACB`。
