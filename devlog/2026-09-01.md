# 2026-09-01

## 已完成

- 重构 `src/waveforms.py`：仅保留恒定波形，增加官方明文 `.pulse` 解析、目录扫描、错误汇总和统一 Pulse 帧。
- V3/V4 控制器接入 `WaveformCatalog`，共用 8 字节波形帧，并支持 A/B 独立时间范围随机任务。
- 配置迁移旧预设名与 `random_interval`，新增常规随机范围和事件触发随机字段。
- 主界面增加独立“波形设置”窗口入口，支持空战/陆战场景、刷新资源、打开目录及保存。
- 主控制器接入常规随机和事件触发随机，事件期间覆盖常规波形。
- README、技术规格、设计规范和开发计划已同步更新。

## 验证

- `python -m py_compile` 通过。
- `PYTHONPATH=. pytest -q`：108 项通过。

## 待办与限制

- 尚未使用真实 DG-LAB 设备验证自定义帧的听感和强度曲线。
- 需要在 PyInstaller 目录包中再次确认 EXE 同级 `waveforms` 文件夹行为。
