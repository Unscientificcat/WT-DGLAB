# 开发日志 — 2026-06-14

## 完成事项

### 陆战 CAS 模式
- [x] 陆战模式下上飞机自动切换 CAS 设置（独立过载范围/波形/通道）
- [x] `CasSettings` 配置组：gforce_min/max, channel_a/b_max, waveform_a/b, random_interval
- [x] 悬浮窗 CAS 场景修复 — 坦克模式下飞机数据正确显示 G 值

### 击杀/死亡/维修事件
- [x] `/hudmsg` 增量解析击杀/坠毁事件（线程安全，独立 requests）
- [x] 零宽字符 `​` 清理
- [x] 玩家名匹配：`玩家 (载具名) 击落了 敌人` 格式
- [x] 空战/陆战事件设置独立分离（`EventSettings` / `TankEventSettings`）
- [x] 维修事件：`is_repairing` 状态检测，维修中持续电击，最长 60s 超时
- [x] 事件优先级：维修 < 击杀/死亡
- [x] 事件期间倒计时显示，到期自动恢复

### 波形系统
- [x] `src/waveforms.py` — 18 种官方波形预设（爬坡、呼吸、潮汐、连击等）
- [x] `WaveformPlayer` 脉冲生成器，支持恒定/波形两种模式
- [x] 每通道独立随机波形切换，可配间隔
- [x] 波形强度上限 100（设备限制，超 100 会丢弃通道数据）

### 悬浮窗
- [x] 透明置顶窗口，`overrideredirect` + `transparentcolor`
- [x] 左键拖动，右键菜单切换大小（大/中/小）
- [x] 分字段缓存 — 6 个标签各自对比，只重绘变化项
- [x] 有效值缓存 — 游戏数据短暂无效时沿用上次值，避免数值与 `--` 跳动
- [x] 刷新节流：每 2 拍（200ms）更新

### 注意事项（原免责声明）
- [x] 创建 `注意事项.txt`，内容保持严肃安全警告
- [x] `src/gui/disclaimer_dialog.py` — 可滚动 Toplevel 对话框
  - 每次启动必弹，确认键固定底部（不会被挤出视口）
  - `**关键字**` 自动高亮（红色加粗，去星号）
  - 关闭窗口 = 退出程序
- [x] 状态栏「注意事项」按钮，随时可查看
- [x] 移除 `disclaimer_accepted` 字段（不再只首次弹出）

### 性能与稳定性修复
- [x] WT 连接状态防抖（连续 3 次失败才显示断开）
- [x] 悬浮窗闪烁修复（分字段缓存 + 有效值缓存双保险）
- [x] 保存按钮崩溃修复（`_overlay_size_var` 属性错误）
- [x] 倒计时步长修复（100ms 节拍匹配 0.1s 递减）
- [x] HTTP 请求全部移到后台线程，解决 GUI 拖拽卡顿
- [x] `127.0.0.1` 替代 `localhost`，消除 DNS 延迟（2s → 7ms）

### 打包
- [x] `build.py` 完善 — 隐藏导入、数据文件、图标支持
- [x] PyInstaller `--onefile` 打包，产物 `WT-DGLAB.exe`（26 MB）
- [x] 输出路径改为项目根目录
- [x] `注意事项.txt` 嵌入 exe，无需额外附带

## 项目文件清单

```
WT-DGLAB/
├── main.py
├── requirements.txt
├── CLAUDE.md
├── build.py
├── 注意事项.txt               # 安全警告（启动时展示）
├── WT-DGLAB.exe              # 打包产物
├── config.json               # 运行时生成
├── docs/
│   ├── requirements.md
│   ├── tech-spec.md
│   ├── design-spec.md
│   └── dev-plan.md
├── devlog/
│   ├── 2026-06-10.md
│   └── 2026-06-14.md         # 本日志
└── src/
    ├── __init__.py
    ├── config_manager.py
    ├── game_reader.py
    ├── coyote_controller.py
    ├── mapping_engine.py
    ├── waveforms.py           # 18 种波形预设
    └── gui/
        ├── __init__.py
        ├── main_window.py
        ├── styles.py
        ├── overlay.py         # 透明悬浮窗
        └── disclaimer_dialog.py  # 注意事项对话框
```

## 待办

- [ ] 用户实测反馈收集
- [ ] 如需图标，放 `icon.ico` 到项目根重新打包
