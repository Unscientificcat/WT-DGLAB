# 开发日志 - 2026-08-04

## 任务

重写战争雷霆 HUD 事件检测，移除多轮修补后分散在 `main.py` 中的游标、跨局和文本解析状态。

## 已完成

- [x] 实现独立 `EventDetector`，持续消费跨局递增的 `lastDmg`。
- [x] 将击杀、被击落和维修边沿检测迁移到新模块。
- [x] 精简 `main.py`，删除重复检测方法和旧状态变量。
- [x] 建立模块级测试，覆盖首次基线、菜单消费、新局首事件、重复消息、维修边沿及请求失败。
- [x] 完成重写范围、模块边界和安全归零责任划分。
- [x] 完整自动测试 `16 passed`，相关文件通过 Python 3.14 语法编译。
- [x] Qt 离屏应用初始化与关闭冒烟测试通过。

## 待办

- [ ] 战争雷霆实机跨局测试。

## DG-LAB 4.x App 适配

- [x] 阅读官方 V4 Socket SDK、Relay 和二维码协议文档。
- [x] 建立 `docs/v4-app-adaptation.md` 开发文档。
- [x] 在右侧连接面板增加 V3/V4 用户选择和 Relay 地址设置。
- [x] 实现 V4 控制器并保留 V3 兼容；保存后无需重启程序即可切换。
- [x] V3/V4 按钮改为点击后立即持久化、切换控制器并刷新二维码，无需再点击“保存设置”。
- [x] 将网络连接启动移出 Qt 主线程，避免 Relay 不可达时界面卡顿。
- [x] 修复 V3 服务停止流程，切换协议时不再遗留 asyncio pending task。
- [x] 官方 V4 Relay 真实握手、二维码生成和关闭验证通过。
- [x] V3 本机服务启动与干净停止验证通过。
- [x] 完整自动测试 `29 passed`，Python 语法检查和 1280×780 布局检查通过。
- [x] 完整应用内点击 V4 后自动连接官方 Relay并刷新二维码验证通过。
- [ ] 使用 DG-LAB 4.x App 与实际郊狼设备联调。

## 程序图标与品牌图片

- [x] 使用项目根目录 `fengmian.jpg` 作为 Qt 窗口和应用程序图标。
- [x] 将页面左上角文字标记替换为同一封面图片，保持 38×38 固定布局。
- [x] 将 `fengmian.jpg` 加入 PyInstaller 数据文件并作为 Windows 可执行文件图标来源。
- [x] 重新生成根目录 `WT-DGLAB.exe`，并从 EXE 资源提取图标确认封面已生效。
- [x] 图标及刷新间隔布局相关回归加入完整测试，最终 `31 passed`。

## 刷新间隔布局调整

- [x] 将刷新间隔从右侧连接设置移动到左侧仪表盘底部。
- [x] 保持 `50..1000 ms` 范围、滚轮保护及原配置保存逻辑。

## 2K 悬浮窗尺寸优化

- [x] 将悬浮窗“大”档字号由 `34 / 17 / 13px` 调整为 `40 / 20 / 15px`。
- [x] 保持“中 / 小”档不变，兼顾 1080P 屏幕的紧凑显示需求。
- [x] 新增字号预设和即时应用回归测试。
- [x] 离屏实测三档约为 `200×89 / 205×101 / 277×133px`，完整自动测试 `33 passed`。
- [x] 已重新生成根目录 `WT-DGLAB.exe`，包含本次悬浮窗大档调整。

## 版本号更新

- [x] 将窗口标题、入口说明、README 和注意事项中的版本号统一更新为 `v1 beta`。
- [x] 新增窗口标题回归断言，保持程序名与 EXE 文件名不变。
- [x] 完整自动测试 `33 passed`，正式发布位置无旧版本号残留。
- [x] 已重新生成根目录 `WT-DGLAB.exe`，打包成功。

## 程序图标更新为 tubiao.jpg

- [x] 将 Qt 窗口、任务栏和页面左上角图标资源替换为根目录 `tubiao.jpg`。
- [x] 为非方形源图增加居中裁切，保持左上角 `38×38px` 固定布局。
- [x] 将 PyInstaller 的数据资源和 Windows EXE 图标来源切换为 `tubiao.jpg`。
- [x] 更新品牌图片回归测试与当前状态文档。
- [x] 完整自动测试 `33 passed`，1280×780 离屏截图确认左上角图标无拉伸或布局偏移。
- [x] 已重新生成根目录 `WT-DGLAB.exe`，并通过 Windows 图标资源接口提取确认新图标已嵌入。

## 任务管理器图标修复

- [x] 定位到 JPG 直接转换和 Windows 同路径图标缓存仍可能导致任务管理器显示旧图标。
- [x] 预先生成包含 `16～256px` 九档尺寸的标准 `tubiao.ico` 和 `256×256px` 的 `tubiao_ui.jpg`。
- [x] Qt 应用与 PyInstaller EXE 统一使用 ICO，并设置新的 Windows AppUserModelID。
- [x] 移除程序及构建过程中的图片裁切/转换逻辑，改为直接加载预制资源。
- [x] 新增 ICO 格式、完整尺寸集合和方形品牌图回归测试。
- [x] 完整自动测试 `34 passed`，最终 EXE 打包成功。
- [x] PyInstaller 规格确认使用 `tubiao.ico`；Windows 关联图标接口提取确认新图标已嵌入。

## GitHub README 重写

- [x] 重写项目首页，覆盖功能、系统要求、V3/V4 使用流程、安全机制、配置、故障排查、架构与源码开发。
- [x] 新增 `v1 beta` 相比 `v0.1` 的完整更新日志，并明确 V4 实机联调状态与 beta 限制。
- [x] 移除无对应 `LICENSE` 文件的 MIT 徽章，增加公开仓库前的隐私与发布检查说明。
- [x] 对照 `Unscientificcat/WT-DGLAB` 旧仓库 README、依赖和 2026-06-10 / 06-14 开发日志，校准 `v0.1` 功能基线。
- [x] README 本地图片、文档链接、代码块、真实克隆地址和版本段落校验通过。

## GitHub 仓库发布副本

- [x] 将源码、测试、文档、开发日志、图标资源和构建脚本复制到根目录 `release/`，原文件未移动或删除。
- [x] 排除个人 `config.json`、诊断日志、Python/pytest 缓存、PyInstaller 中间产物、生成的 spec 和 280 MB EXE。
- [x] 在副本中加入 `.gitignore`，防止后续误提交运行配置、日志、缓存与超大构建产物。
- [x] 逐文件 SHA-256 对比通过；从副本运行完整自动测试 `34 passed`。
