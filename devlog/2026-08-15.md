# 开发日志 - 2026-08-15

## 托盘、配置持久化与图标更新

- [x] 确认配置保存到 EXE 同目录，缺失时自动生成。
- [x] 确认关闭到托盘和托盘退出时自动保存当前界面设置。
- [x] 确认 `tubiao1.jpg` 用于全部程序及界面图标位置。
- [x] 建立开发文档 `docs/tray-persistence-icon-update.md`。
- [x] 实现配置路径和缺失时自动生成。
- [x] 配置保存改为临时文件原子替换。
- [x] 实现设置自动采集与保存，无效 Relay 地址自动保留旧值。
- [x] 实现关闭隐藏、双击恢复、右键菜单退出的系统托盘生命周期。
- [x] 转换并替换全部图标资源，ICO 包含 9 档标准尺寸。
- [x] 托盘阶段新增 4 个回归测试，完整测试共 41 项。
- [x] 全项目静态编译通过，配置保存与重新加载独立验证通过。
- [x] 托盘阶段根目录与 `release` 完整自动测试均为 `41 passed`。
- [x] 使用 Python 3.14 / PyInstaller 6.20.0 重新打包成功。
- [x] Windows 托盘和 EXE 实机验证（关闭隐藏、重复启动恢复窗口）。

## 单实例与重复启动

- [x] 使用稳定的 Windows 命名互斥锁阻止第二套业务实例启动。
- [x] 使用 Qt 本地 IPC 将重复启动转换为“恢复并激活已有主窗口”。
- [x] 增加激活请求应答和启动阶段连接重试，降低消息丢失风险。
- [x] 第二实例不初始化 `App`、游戏轮询、郊狼连接或系统托盘。
- [x] 新增 3 个单实例回归测试，完整自动测试更新为 `44 passed`。
- [x] 重新打包并完成双击 EXE 的双进程实机验证：基线 2 个 PyInstaller 进程，第二次启动后仍为 2 个，已有窗口句柄恢复且响应正常。

## 维修事件被击杀覆盖后恢复

- [x] 定位同轮维修与击杀发生时，维修上升沿被击杀优先级消费的问题。
- [x] 击杀或死亡事件结束后重新检查最新坦克维修状态。
- [x] 仍在维修时直接切换到维修强度与波形，不要求玩家放弃后重新维修。
- [x] 已停止维修或维修功能关闭时恢复常规映射。
- [x] 新增 3 个回归测试，完整自动测试更新为 `47 passed`。
- [x] 根目录与 `release` 完整自动测试均为 `47 passed`。
- [x] 同步 `release` 并重新打包 EXE，构建成功。

## 陆战 CAS 触发开关与事件修复

- [x] 增加并持久化“启用 CAS 触发”开关，默认开启兼容旧配置。
- [x] 关闭 CAS 时将上飞机后的过载强度归零，并禁止击杀/坠毁事件。
- [x] 陆战 CAS 使用陆战模式的玩家昵称、击杀和坠毁参数，显示仍区分飞机语义。
- [x] 新增 CAS 配置、事件选择和输出门控测试。
- [x] 完整自动测试更新为 `51 passed`。
- [x] 根目录与 `release` 完整自动测试均为 `51 passed`。
- [x] 同步 `release` 与 PyInstaller 重新打包，构建成功。

## v1 beta_1 发布整理

- [x] 将今晚完成的系统托盘、配置持久化、单实例激活、维修事件恢复和陆战 CAS 更新归档为 `郊狼雷霆 v1 beta_1`。
- [x] 统一主窗口标题、Windows 应用标识、任务栏/任务管理器图标对应的版本标识。
- [x] PyInstaller 产物改名为 `WT-DGLAB v1 beta_1.exe`。
- [x] README、开发计划和专项开发文档同步更新版本及发布说明。
- [x] 将源码、测试、文档、图标和新版 EXE 复制到 `release`，排除个人配置与诊断日志。
- [x] 根目录与 `release` 自动化测试均为 `51 passed`。
